# Clean Architecture Template For My Personal Use

Significantly trimmed and optimised version of Milan Jovanovic CA Template

To install template clone this repo and run following command in cloned folder

`dotnet new install .`

Then use this command to scaffold new Clean Architecture project

`dotnet new ca-min -n ProjectName`
